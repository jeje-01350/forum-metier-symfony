{{ form_start(form, {'attr': {'class': 'mt-8'}}) }}
    <div class="mb-4">
        {{ form_widget(form, {'attr': {'class': 'w-full p-3 border rounded'}}) }}
    </div>
    <button class="bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue active:bg-blue-800" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
