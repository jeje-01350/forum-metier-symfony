{% extends 'base.html.twig' %}

{% block title %}Lycee{% endblock %}

{% block body %}
    <h1>Lycee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lycee.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lycee.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lycee_index') }}">back to list</a>

    <a href="{{ path('app_lycee_edit', {'id': lycee.id}) }}">edit</a>

    {{ include('lycee/_delete_form.html.twig') }}
{% endblock %}
