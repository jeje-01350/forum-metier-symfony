{% extends 'base.html.twig' %}

{% block title %}Lycee index{% endblock %}

{% block body %}
    <h1>Lycee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lycee in lycees %}
            <tr>
                <td>{{ lycee.id }}</td>
                <td>{{ lycee.nom }}</td>
                <td>
                    <a href="{{ path('app_lycee_show', {'id': lycee.id}) }}">show</a>
                    <a href="{{ path('app_lycee_edit', {'id': lycee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lycee_new') }}">Create new</a>
{% endblock %}
