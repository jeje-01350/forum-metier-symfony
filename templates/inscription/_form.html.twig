{{ form_start(form, {'attr': {'class': 'my-form-class'}}) }}
    {{ form_widget(form, {'attr': {'class': 'my-input-class'}}) }}
    <button class="bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue active:bg-blue-800">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
