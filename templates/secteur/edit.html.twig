{% extends 'base.html.twig' %}

{% block title %}Edit Secteur{% endblock %}

{% block body %}
    <div class="container mx-auto mt-8">
        <div class="bg-white p-8 shadow-md rounded-md">
            <h1 class="text-2xl font-bold mb-4">Edit Secteur</h1>

            {{ include('secteur/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="mt-4">
                <a href="{{ path('app_secteur_index') }}" class="text-blue-500 hover:underline">Back to List</a>
            </div>

            {{ include('secteur/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
