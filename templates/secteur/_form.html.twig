{{ form_start(form, {'attr': {'class': 'my-form-class'}}) }}

    <div class="mb-4">
        {{ form_widget(form, {'attr': {'class': 'my-input-class p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300 w-full'}}) }}
    </div>

    <button class="bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-700" type="submit">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
