{% extends 'base.html.twig' %}

{% block title %}Edit Lyceen{% endblock %}

{% block body %}
    <div class="max-w-md mx-auto mt-8 bg-white p-8 rounded shadow-md">
        <h1 class="text-2xl font-bold mb-4">Edit Lyceen</h1>

        {{ include('lyceen/_form.html.twig', {'button_label': 'Update'}) }}
        {% if is_granted('ROLE_ADMIN') %}

        <a href="{{ path('app_lyceen_index') }}" class="block mt-4 text-blue-500 hover:underline">Back to List</a>
        {% endif %}
    </div>
{% endblock %}
