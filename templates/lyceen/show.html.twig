{% extends 'base.html.twig' %}

{% block title %}Lyceen{% endblock %}

{% block body %}
    <h1>Lyceen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lyceen.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lyceen.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ lyceen.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ lyceen.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ lyceen.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lyceen_index') }}">back to list</a>

    <a href="{{ path('app_lyceen_edit', {'id': lyceen.id}) }}">edit</a>

    {{ include('lyceen/_delete_form.html.twig') }}
{% endblock %}
