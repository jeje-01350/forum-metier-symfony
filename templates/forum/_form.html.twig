{{ form_start(form, {'attr': {'class': 'bg-white-200 p-4 rounded shadow-md'}}) }}
    {{ form_widget(form, {'class': 'bg-gray-200'}) }}
    <button class="btn bg-blue-500 text-white py-2 px-4 rounded mt-4 hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue active:bg-blue-800">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}

