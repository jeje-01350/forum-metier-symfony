{{ form_start(form, {'attr': {'class': 'my-form'}}) }}
    {{ form_widget(form, {'attr': {'class': 'my-input p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300'}}) }}
    <button class="bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue active:bg-blue-800" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
